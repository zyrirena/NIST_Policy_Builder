[build]
buildCommand = "npm install && cd backend && npm install && node src/utils/migrate.js && node src/seeds/seed.js && cd ../frontend && npm install && npm run build"

[deploy]
startCommand = "node backend/src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
